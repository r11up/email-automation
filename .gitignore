# graph_token.json holds your Microsoft OAuth access + refresh tokens.
# It is set to chmod 600 (only you can read it) by the script.
# NEVER commit this file to git.
graph_token.json

# OS X
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
